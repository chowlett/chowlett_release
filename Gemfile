# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in chowlett_release.gemspec
gemspec

gem 'rake', '~> 13.0'

gem 'ostruct'
gem 'octokit'

group :development, :test do
  gem 'minitest', '~> 5.16'

  gem 'rubocop'
end

group :test do
  gem 'rails', '~> 8.0' # or your required version
end
